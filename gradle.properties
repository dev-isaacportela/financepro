org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.configuration-cache=true

android.useAndroidX=true
android.nonTransitiveRClass=true

kotlin.code.style=official

# O JDK que roda o Gradle NÃO é configurado aqui de propósito: um caminho
# absoluto quebraria em toda outra máquina. O Android Studio usa seu JBR
# automaticamente; o CI fixa JDK 21 via setup-java. AGP 8.13 não suporta
# rodar sobre JDK 24.
