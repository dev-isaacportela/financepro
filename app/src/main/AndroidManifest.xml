<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
      REQ-SEC-007 — a rede entra para UMA coisa: ler o CDI da série pública do
      Banco Central (T-050, ADR-012). A requisição é um GET sem parâmetro, sem
      cabeçalho de identificação e sem corpo — nenhum dado do usuário sai do
      aparelho, e não há para onde ele sair: api.bcb.gov.br é o único host que
      o app conhece.

      A garantia que a ausência desta linha dava passou para duas guardas que
      valem mais que ela: o ManifestTest da T-019 exige o conjunto EXATO de
      permissões do manifesto MESCLADO (qualquer uma a mais, inclusive vinda de
      AAR transitivo, reprova), e tools/trace.py reprova o build se aparecer em
      src/main uma URL para qualquer outro host.

      Open Finance continua fora, e continua sendo assunto da T-049 na F4:
      o que entrou aqui é um número público, não a conta de ninguém.
    -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!--
      REQ-SEC-003 — bloqueio biométrico opcional, com fallback para a credencial
      do aparelho. Declarada nominalmente aqui e não herdada em silêncio da
      androidx.biometric: o ManifestTest da T-019 proíbe INTERNET pelo nome, não
      "toda permissão", justamente para esta caber sem afrouxar aquela guarda.
    -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />

    <application
        android:name=".FinanceApp"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.MobileFinance">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.MobileFinance">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
